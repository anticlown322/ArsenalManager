<UserControl x:Class="ArsenalManager.UI.MVVM.Views.ArsenalManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:ArsenalManager.UI.MVVM.ViewModels"
             d:DataContext="{d:DesignInstance Type=viewModels:ArsenalManagerViewModel}"
             mc:Ignorable="d"
             d:DesignHeight="650"
             d:DesignWidth="1130">
    <Grid>
        <!--Grid setup-->
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
    </Grid>
</UserControl>